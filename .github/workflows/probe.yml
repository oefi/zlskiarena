name: Open-Meteo 2026 Probe

on:
  workflow_dispatch:
  # Disabled — run manually by temporarily restoring workflow_dispatch
  # workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests
      - run: python3 probe_openmeteo_2026.py
